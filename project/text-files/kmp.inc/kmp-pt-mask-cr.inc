
# Both settings support a common definition of ENPT and ITPT.
# If writing a binary KMP file, the settings 1 and 2 are masked
# (AND operation) by the following values:
@mask-1 = 0x%04x
@mask-2 = 0x%04x
